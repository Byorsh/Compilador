;/StartHeader
INCLUDE macros.mac
DOSSEG
.MODEL SMALL
STACK 100h
.DATA
			x DW '', '$' 
			t1 DW '' ? 
			t2 DW '' ? 
.CODE
.386
BEGIN:
			MOV AX, @DATA
			MOV DS, AX
CALL COMPI
			MOV AX, 4C00H
			INT 21H
COMPI PROC
			I_ASIGNAR 0, t1
			DIVIDE 0, x, t2
			I_ASIGNAR x, t2

	ret
COMPI ENDP
END BEGIN
